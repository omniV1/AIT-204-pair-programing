# Deep Learning Framework
torch>=2.2.0
# (CPU-only build — for GPU support install torch with CUDA extras from pytorch.org)

# API Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Data Validation
pydantic>=2.0.0

# Scientific Computing
numpy>=1.24.0

# Visualization (used by train.py and architecture endpoint)
matplotlib>=3.7.0

# Environment Variables
python-dotenv>=1.0.0
